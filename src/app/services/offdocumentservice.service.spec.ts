/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { OffdocumentserviceService } from './offdocumentservice.service';

describe('Service: Offdocumentservice', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [OffdocumentserviceService]
    });
  });

  it('should ...', inject([OffdocumentserviceService], (service: OffdocumentserviceService) => {
    expect(service).toBeTruthy();
  }));
});
